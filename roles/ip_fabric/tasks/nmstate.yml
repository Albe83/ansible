---
# tasks/nmstate.yml
# Tutto ciò che riguarda nmstate: install, config, render dei file e avvio del servizio.

- name: Ensure nmstate package is installed
  package:
    name: nmstate
    state: present
  become: true

- name: Ensure NetworkManager is running (nmstate requires it)
  service:
    name: NetworkManager
    state: started
    enabled: true
  become: true

- name: Ensure nmstate config directory exists
  file:
    path: /etc/nmstate
    state: directory
    owner: root
    group: root
    mode: '0755'
  become: true

- name: Configure nmstate service options
  copy:
    dest: /etc/nmstate/nmstate.conf
    owner: root
    group: root
    mode: '0644'
    content: |
      [service]
      keep_state_file_after_apply = true
  become: true

# ---------- RENDER DEGLI STATE FILE ----------

- name: Render loopback0 NMState file
  template:
    src: nm_loopback0.yaml.j2
    dest: /etc/nmstate/ip-fabric-loopback0.yml
    owner: root
    group: root
    mode: '0644'
  become: true

- name: Render fabric NMState files (blue/green)
  template:
    src: nm_fabric_iface.yaml.j2
    dest: "/etc/nmstate/ip-fabric-{{ fabric.name }}.yml"
    owner: root
    group: root
    mode: '0644'
  loop: "{{ ip_fabric.fabrics }}"
  loop_control:
    loop_var: fabric
  become: true

# ---------- AVVIO / RIAPPLICAZIONE ----------

- name: Enable nmstate.service so configs are applied on every boot
  service:
    name: nmstate
    enabled: true
  b
